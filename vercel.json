{
  "buildCommand": "cd concesionario/frontend && npm install && npm run build",
  "outputDirectory": "concesionario/frontend/dist",
  "rewrites": [
    {
      "source": "/:path((?!.*\\.).*)",
      "destination": "/index.html"
    }
  ]
}
